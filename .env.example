# 部署配置
PORT=80                      # 前端应用暴露的端口
DOCKER_REGISTRY=michael0807   # Docker 镜像仓库
TAG=latest                   # 镜像标签

# 安全配置
JWT_SECRET=changeThisToASecureRandomStringInProduction  # JWT 密钥（生产环境必须更改）
JWT_EXPIRATION=86400         # JWT 过期时间（秒） 